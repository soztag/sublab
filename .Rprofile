#### -- Packrat Autoloader (version 0.4.8-1) -- ####
getwd()
source(file = "packrat/init.R")
#### -- End Packrat Autoloader -- ####
if (isTRUE(interactive() & Sys.getenv("RSTUDIO") == 1)) {
  # source only when we're in rstudio
  source(file = "setup.R")
}
